<phone:PhoneApplicationPage
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Konz_MyMovies_Core="clr-namespace:Konz.MyMovies.Core;assembly=Konz.MyMovies.Core" 
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls" 
    x:Class="Konz.MyMovies.UI.MainPage"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="696"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True" FontSize="{StaticResource PhoneFontSizeLarge}">

    <!--Sample code showing usage of ApplicationBar-->
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="Cambiar de Ciudad" Click="MenuCityChange_Click"/>
                <shell:ApplicationBarMenuItem Text="Cambiar de Complejo" Click="MenuComplexChange_Click"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

    <phone:PhoneApplicationPage.FontFamily>
        <StaticResource ResourceKey="PhoneFontFamilyNormal"/>
    </phone:PhoneApplicationPage.FontFamily>
    <phone:PhoneApplicationPage.Foreground>
        <StaticResource ResourceKey="PhoneForegroundBrush"/>
    </phone:PhoneApplicationPage.Foreground>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent" Loaded="LayoutRoot_Loaded" >
        <Grid.RowDefinitions>
            <RowDefinition Height="678*"/>
            <RowDefinition Height="90"/>
        </Grid.RowDefinitions>
        <controls:Pivot Name="pvtTheaters" Title="Complejos" VerticalAlignment="Top" ItemsSource="{Binding}" SelectionChanged="pvtTheaters_SelectionChanged" >
            <controls:Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" TextWrapping="Wrap" FontSize="{StaticResource PhoneFontSizeExtraLarge}" Foreground="{StaticResource PhoneAccentBrush}"></TextBlock>
                </DataTemplate>
            </controls:Pivot.HeaderTemplate>
            <controls:Pivot.ItemTemplate>
                <DataTemplate>
                    <ListBox ItemsSource="{Binding Movies}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Height="auto" Width="auto" Margin="0 0 0 10" >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.325*"/>
                                        <ColumnDefinition Width="0.675*"/>
                                    </Grid.ColumnDefinitions>
                                    <Image Source="{Binding PictureImg}" Width="100" HorizontalAlignment="Left" VerticalAlignment="Top" d:LayoutOverrides="VerticalAlignment"/>
                                    <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Grid.Column="1" Margin="5,0,0,0">
                                        <TextBlock Text="{Binding Name}" FontSize="{StaticResource PhoneFontSizeLarge}" TextWrapping="Wrap" Foreground="{StaticResource PhoneAccentBrush}"/>
                                        <TextBlock Text="{Binding Showtimes}" FontSize="{StaticResource PhoneFontSizeMediumLarge}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DataTemplate>
            </controls:Pivot.ItemTemplate>
        </controls:Pivot>
        <Slider Name="sldFromTime" Minimum="0" Maximum="840" Grid.Row="1" Height="90" Margin="0,0,101,0" ValueChanged="Slider_ValueChanged" />
        <TextBlock Grid.Row="1" Margin="0,0,12,0" Name="txtTimeFrom" Text="09:00" FontSize="{StaticResource PhoneFontSizeLarge}" TextAlignment="Center" Height="49" VerticalAlignment="Top" HorizontalAlignment="Right" Width="83" />

        <Popup x:Name="popSelectTheater" Height="600" Width="400" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid Background="#FF1A69AC" Height="600" Width="400" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Elige tu Cine" Grid.Row="0" Margin="20,10,0,0"/>
                <ListBox Name="lstTheaters" Grid.Row="1" SelectionChanged="Theater_SelectionChanged" Margin="15" ItemsSource="{Binding}" Background="Black" DisplayMemberPath="Name"/>
            </Grid>
        </Popup>
    </Grid>

</phone:PhoneApplicationPage>