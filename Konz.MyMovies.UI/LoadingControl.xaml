<UserControl x:Class="Konz.MyMovies.UI.LoadingControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}" Loaded="UserControl_Loaded">

    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneBackgroundBrush}" >
        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Center" >
            <TextBlock  Name="txtMessage" Text="Loading..." FontSize="{StaticResource PhoneFontSizeMediumLarge}" TextWrapping="Wrap" Margin="15" TextAlignment="Center" />
            <ProgressBar HorizontalAlignment="Stretch" Name="pgbLoading" IsIndeterminate="True" IsTabStop="False" />
        </StackPanel>
    </Grid>
</UserControl>
